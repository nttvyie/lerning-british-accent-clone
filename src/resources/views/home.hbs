<div class="mt-4">
    <div class="row">

        {{#each courses}}
        <div class="col-sm-6 col-lg-4 card-course-item">
            <div class="card">
                <a href="/courses/{{this.slug}}">
                    <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}">
                </a>
                <div class="card-body">
                    <h5 class="card-title two-line-paragraph">{{this.name}}</h5>
                    <p class="card-text three-line-paragraph">{{this.description}}</p>
                    <a href="#" class="btn btn-primary">Học ngay</a>
                </div>
            </div>
        </div>
        {{/each}}

    </div>
</div>